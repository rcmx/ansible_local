---
# Development environment configuration

# Development packages configuration
# Different package names for different operating systems
dev_packages_debian:
  - curl
  - build-essential

dev_packages_rhel:
  - curl
  - "@development-tools"

dev_packages_arch:
  - curl
  - base-devel
