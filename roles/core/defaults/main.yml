---
# User Configuration
# Primary user for workstation configuration (non-root interactive user)
primary_user: velez
primary_user_home: "/home/{{ primary_user }}"

# SSH Keys for user provisioning
# These keys are used to configure passwordless access for system automation users

# Ansible system user SSH key
# Used for ansible-pull automation and remote management
ansible_ssh_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExkp/21VsASdw1ujck9XolFvfNGqqqUqbXHpM0RNbJo ansible manage"

# Primary user SSH key
# Used for primary user account access (default: velez user)
# Override this variable to use a different SSH key for the primary user
primary_user_ssh_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOH+GDuX+7RjQhBp5CM0vJRH6s91BrYvm4boNyt6MTPr velez identity"

# Cron configuration
# Cron service names vary by OS family
cron_packages_debian: cron
cron_packages_rhel: [cronie, crontabs]
cron_packages_arch: cronie
